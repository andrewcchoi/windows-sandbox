# Generated by sandbox plugin (Basic mode)
# Auto-detected: Python + Node.js project with database needs

services:
  app:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - .:/workspace:cached
    environment:
      - DATABASE_URL=postgresql://sandbox_user:devpassword@postgres:5432/sandbox_dev
      - REDIS_URL=redis://redis:6379
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - demo-basic-network

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: sandbox_user
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: sandbox_dev
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sandbox_user -d sandbox_dev"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - demo-basic-network

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - demo-basic-network

volumes:
  postgres-data:

networks:
  demo-basic-network:
    driver: bridge
