<<<<<<< HEAD
# YOLO Mode Environment Template
# Full customization with all available configuration options
# This is a comprehensive template - customize to your needs
# Copy to .env and remove unused sections

# ============================================================================
# Container Configuration
# ============================================================================

# Timezone (examples: UTC, America/New_York, Europe/London, Asia/Tokyo)
TZ=UTC

# Firewall mode (disabled, permissive, strict, custom)
FIREWALL_MODE={{FIREWALL_MODE}}
FIREWALL_LOG_LEVEL={{FIREWALL_LOG_LEVEL}}

# Claude Code version
CLAUDE_CODE_VERSION={{CLAUDE_CODE_VERSION}}

# ============================================================================
# Build Configuration (Docker Build Args)
# ============================================================================

# Base image selection
BASE_IMAGE={{BASE_IMAGE}}

# Language versions
PYTHON_VERSION={{PYTHON_VERSION}}
NODE_VERSION={{NODE_VERSION}}
GO_VERSION={{GO_VERSION}}
RUST_VERSION={{RUST_VERSION}}
JAVA_VERSION={{JAVA_VERSION}}
RUBY_VERSION={{RUBY_VERSION}}
PHP_VERSION={{PHP_VERSION}}

# Debian configuration
DEBIAN_FRONTEND=noninteractive

# ============================================================================
# Runtime Environment
# ============================================================================

# Application environment
NODE_ENV={{NODE_ENV}}
RAILS_ENV={{RAILS_ENV}}
FLASK_ENV={{FLASK_ENV}}
DJANGO_SETTINGS_MODULE={{DJANGO_SETTINGS_MODULE}}

# Python configuration
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1
PYTHONPATH={{PYTHONPATH}}

# Go configuration
GOPATH={{GOPATH}}
GOBIN={{GOBIN}}
GOCACHE={{GOCACHE}}

# Rust configuration
RUST_BACKTRACE={{RUST_BACKTRACE}}
CARGO_HOME={{CARGO_HOME}}

# Java configuration
JAVA_HOME={{JAVA_HOME}}
MAVEN_HOME={{MAVEN_HOME}}
GRADLE_HOME={{GRADLE_HOME}}

# Ruby configuration
GEM_HOME={{GEM_HOME}}
BUNDLE_PATH={{BUNDLE_PATH}}

# System PATH
PATH={{PATH}}

# ============================================================================
# Database Configuration
# ============================================================================

# PostgreSQL
POSTGRES_USER={{POSTGRES_USER}}
POSTGRES_PASSWORD={{POSTGRES_PASSWORD}}  # Use input variable: ${input:dbPassword}
POSTGRES_DB={{POSTGRES_DB}}
POSTGRES_HOST={{POSTGRES_HOST}}
POSTGRES_PORT={{POSTGRES_PORT}}
POSTGRES_SSLMODE={{POSTGRES_SSLMODE}}
DATABASE_URL={{DATABASE_URL}}

# MySQL
MYSQL_ROOT_PASSWORD={{MYSQL_ROOT_PASSWORD}}
MYSQL_DATABASE={{MYSQL_DATABASE}}
MYSQL_USER={{MYSQL_USER}}
MYSQL_PASSWORD={{MYSQL_PASSWORD}}
MYSQL_HOST={{MYSQL_HOST}}
MYSQL_PORT={{MYSQL_PORT}}

# MongoDB
MONGO_INITDB_ROOT_USERNAME={{MONGO_INITDB_ROOT_USERNAME}}
MONGO_INITDB_ROOT_PASSWORD={{MONGO_INITDB_ROOT_PASSWORD}}
MONGO_INITDB_DATABASE={{MONGO_INITDB_DATABASE}}
MONGO_HOST={{MONGO_HOST}}
MONGO_PORT={{MONGO_PORT}}
MONGODB_URI={{MONGODB_URI}}

# SQLite
SQLITE_DB_PATH={{SQLITE_DB_PATH}}

# MariaDB
MARIADB_ROOT_PASSWORD={{MARIADB_ROOT_PASSWORD}}
MARIADB_DATABASE={{MARIADB_DATABASE}}
MARIADB_USER={{MARIADB_USER}}
MARIADB_PASSWORD={{MARIADB_PASSWORD}}

# ============================================================================
# Cache Configuration
# ============================================================================

# Redis
REDIS_HOST={{REDIS_HOST}}
REDIS_PORT={{REDIS_PORT}}
REDIS_PASSWORD={{REDIS_PASSWORD}}  # Use Docker secret or input variable
REDIS_DB={{REDIS_DB}}
REDIS_URL={{REDIS_URL}}

# Memcached
MEMCACHED_HOST={{MEMCACHED_HOST}}
MEMCACHED_PORT={{MEMCACHED_PORT}}
MEMCACHED_SERVERS={{MEMCACHED_SERVERS}}

# ============================================================================
# Message Queue and Event Streaming
# ============================================================================

# RabbitMQ
RABBITMQ_DEFAULT_USER={{RABBITMQ_DEFAULT_USER}}
RABBITMQ_DEFAULT_PASS={{RABBITMQ_DEFAULT_PASS}}
RABBITMQ_HOST={{RABBITMQ_HOST}}
RABBITMQ_PORT={{RABBITMQ_PORT}}
RABBITMQ_MANAGEMENT_PORT={{RABBITMQ_MANAGEMENT_PORT}}
RABBITMQ_URL={{RABBITMQ_URL}}

# Kafka
KAFKA_BROKER={{KAFKA_BROKER}}
KAFKA_ZOOKEEPER={{KAFKA_ZOOKEEPER}}
KAFKA_TOPIC={{KAFKA_TOPIC}}
KAFKA_GROUP_ID={{KAFKA_GROUP_ID}}

# NATS
NATS_URL={{NATS_URL}}
NATS_CLUSTER_ID={{NATS_CLUSTER_ID}}

# ============================================================================
# Search and Analytics
# ============================================================================

# Elasticsearch
ELASTICSEARCH_HOST={{ELASTICSEARCH_HOST}}
ELASTICSEARCH_PORT={{ELASTICSEARCH_PORT}}
ELASTICSEARCH_URL={{ELASTICSEARCH_URL}}

# Kibana
KIBANA_HOST={{KIBANA_HOST}}
KIBANA_PORT={{KIBANA_PORT}}

# ============================================================================
# Cloud Provider Configuration
# ============================================================================

# AWS - NEVER set credentials as ENV! Use secret mounts or host mount
AWS_REGION={{AWS_REGION}}
AWS_PROFILE={{AWS_PROFILE}}
# AWS_ACCESS_KEY_ID - USE DOCKER SECRET OR INPUT VARIABLE ONLY
# AWS_SECRET_ACCESS_KEY - USE DOCKER SECRET OR INPUT VARIABLE ONLY

# Google Cloud Platform - NEVER set key as ENV!
GCP_PROJECT_ID={{GCP_PROJECT_ID}}
GCP_REGION={{GCP_REGION}}
# GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/gcp_key.json

# Azure
AZURE_SUBSCRIPTION_ID={{AZURE_SUBSCRIPTION_ID}}
AZURE_TENANT_ID={{AZURE_TENANT_ID}}
AZURE_RESOURCE_GROUP={{AZURE_RESOURCE_GROUP}}

# DigitalOcean
DO_TOKEN={{DO_TOKEN}}  # Use input variable: ${input:doToken}
DO_REGION={{DO_REGION}}

# ============================================================================
# API Keys and Authentication
# ============================================================================

# CRITICAL: Use VS Code input variables with password:true for ALL API keys
# Never commit real API keys! These are placeholders for input variables

# Version Control
GITHUB_TOKEN={{GITHUB_TOKEN}}  # Use: ${input:githubToken}
GITLAB_TOKEN={{GITLAB_TOKEN}}  # Use: ${input:gitlabToken}
BITBUCKET_TOKEN={{BITBUCKET_TOKEN}}  # Use: ${input:bitbucketToken}

# AI/ML Services
OPENAI_API_KEY={{OPENAI_API_KEY}}  # Use: ${input:openaiKey}
ANTHROPIC_API_KEY={{ANTHROPIC_API_KEY}}  # Use: ${input:anthropicKey}
HUGGINGFACE_TOKEN={{HUGGINGFACE_TOKEN}}  # Use: ${input:huggingfaceToken}
COHERE_API_KEY={{COHERE_API_KEY}}

# Payment Processing
STRIPE_SECRET_KEY={{STRIPE_SECRET_KEY}}  # Use Docker secret
STRIPE_PUBLISHABLE_KEY={{STRIPE_PUBLISHABLE_KEY}}
PAYPAL_CLIENT_ID={{PAYPAL_CLIENT_ID}}
PAYPAL_CLIENT_SECRET={{PAYPAL_CLIENT_SECRET}}  # Use Docker secret

# Communication Services
SENDGRID_API_KEY={{SENDGRID_API_KEY}}  # Use Docker secret
TWILIO_ACCOUNT_SID={{TWILIO_ACCOUNT_SID}}
TWILIO_AUTH_TOKEN={{TWILIO_AUTH_TOKEN}}  # Use Docker secret
SLACK_BOT_TOKEN={{SLACK_BOT_TOKEN}}  # Use input variable
DISCORD_BOT_TOKEN={{DISCORD_BOT_TOKEN}}

# Maps and Location
GOOGLE_MAPS_API_KEY={{GOOGLE_MAPS_API_KEY}}
MAPBOX_ACCESS_TOKEN={{MAPBOX_ACCESS_TOKEN}}

# Analytics and Monitoring
GOOGLE_ANALYTICS_ID={{GOOGLE_ANALYTICS_ID}}
MIXPANEL_TOKEN={{MIXPANEL_TOKEN}}
SEGMENT_WRITE_KEY={{SEGMENT_WRITE_KEY}}

# Error Tracking
SENTRY_DSN={{SENTRY_DSN}}  # Use: ${input:sentryDsn}
ROLLBAR_ACCESS_TOKEN={{ROLLBAR_ACCESS_TOKEN}}

# Package Registries (use Docker secrets during build!)
NPM_TOKEN={{NPM_TOKEN}}  # Mount as secret in Dockerfile
PYPI_TOKEN={{PYPI_TOKEN}}  # Mount as secret in Dockerfile
GEM_TOKEN={{GEM_TOKEN}}

# ============================================================================
# SSL/TLS Configuration
# ============================================================================

# SSL certificates (mount as Docker secrets)
SSL_CERT_PATH={{SSL_CERT_PATH}}
SSL_KEY_PATH={{SSL_KEY_PATH}}
SSL_CA_PATH={{SSL_CA_PATH}}

# ============================================================================
# Monitoring and Observability
# ============================================================================

# Logging
LOG_LEVEL={{LOG_LEVEL}}
LOG_FORMAT={{LOG_FORMAT}}
LOG_OUTPUT={{LOG_OUTPUT}}

# Metrics
PROMETHEUS_PORT={{PROMETHEUS_PORT}}
GRAFANA_ADMIN_PASSWORD={{GRAFANA_ADMIN_PASSWORD}}

# Tracing
JAEGER_AGENT_HOST={{JAEGER_AGENT_HOST}}
JAEGER_AGENT_PORT={{JAEGER_AGENT_PORT}}

# ============================================================================
# Application-Specific Configuration
# ============================================================================

# Application URLs
APP_URL={{APP_URL}}
API_URL={{API_URL}}
FRONTEND_URL={{FRONTEND_URL}}
BACKEND_URL={{BACKEND_URL}}

# Session and Security
SESSION_SECRET={{SESSION_SECRET}}  # Use input variable
JWT_SECRET={{JWT_SECRET}}  # Use Docker secret
ENCRYPTION_KEY={{ENCRYPTION_KEY}}  # Use Docker secret

# Feature Flags
FEATURE_FLAG_SERVICE={{FEATURE_FLAG_SERVICE}}
FEATURE_FLAGS={{FEATURE_FLAGS}}

# ============================================================================
# Development Tools
# ============================================================================

# Hot Reload
HOT_RELOAD={{HOT_RELOAD}}
WATCH_FILES={{WATCH_FILES}}

# Debug
DEBUG={{DEBUG}}
VERBOSE={{VERBOSE}}

# Testing
TEST_DATABASE_URL={{TEST_DATABASE_URL}}
COVERAGE_THRESHOLD={{COVERAGE_THRESHOLD}}

# ============================================================================
# Resource Limits (informational - set in docker-compose.yml)
# ============================================================================

# CPU_LIMIT={{CPU_LIMIT}}
# MEMORY_LIMIT={{MEMORY_LIMIT}}
# SWAP_LIMIT={{SWAP_LIMIT}}

# ============================================================================
# Custom Application Variables
# ============================================================================

# Add your custom variables below
# CUSTOM_VAR_1={{CUSTOM_VAR_1}}
# CUSTOM_VAR_2={{CUSTOM_VAR_2}}
# CUSTOM_VAR_3={{CUSTOM_VAR_3}}

# ============================================================================
# SECURITY CHECKLIST - COMPLETE BEFORE PRODUCTION!
# ============================================================================

# [ ] Add .env to .gitignore
# [ ] Replace ALL {{PLACEHOLDER}} values
# [ ] Remove unused sections
# [ ] Change ALL default passwords
# [ ] Configure VS Code input variables for API keys
# [ ] Set up Docker secrets for sensitive credentials
# [ ] Mount cloud provider credentials from host
# [ ] Enable strict firewall mode
# [ ] Configure custom firewall allowlist
# [ ] Review and update allowed domains
# [ ] Test credential rotation procedures
# [ ] Document required secrets for team
# [ ] Set up secret management system (Vault, Secrets Manager, etc.)
# [ ] Configure MCP servers with proper authentication
# [ ] Review container resource limits
# [ ] Set up monitoring and alerting
# [ ] Test backup and recovery procedures

# ============================================================================
# Documentation References
# ============================================================================

# For comprehensive guidance:
# - docs/VARIABLES.md - Variable types and usage patterns
# - docs/SECRETS.md - Secure credential handling
# - docs/MODES.md - Mode comparison and selection
# - docs/security-model.md - Overall security architecture
# - data/variables.json - Complete variable catalog
# - data/secrets.json - Secret handling patterns

# YOLO Mode = Maximum Control + Maximum Responsibility
# With great power comes great responsibility - secure your credentials!
=======
# YOLO Mode Environment Template
# Full customization with all available configuration options
# This is a comprehensive template - customize to your needs
# Copy to .env and remove unused sections

# ============================================================================
# Container Configuration
# ============================================================================

# Timezone (examples: UTC, America/New_York, Europe/London, Asia/Tokyo)
TZ=UTC

# Firewall mode (disabled, permissive, strict, custom)
FIREWALL_MODE={{FIREWALL_MODE}}
FIREWALL_LOG_LEVEL={{FIREWALL_LOG_LEVEL}}

# Claude Code version
CLAUDE_CODE_VERSION={{CLAUDE_CODE_VERSION}}

# ============================================================================
# Build Configuration (Docker Build Args)
# ============================================================================

# Base image selection
BASE_IMAGE={{BASE_IMAGE}}

# Language versions
PYTHON_VERSION={{PYTHON_VERSION}}
NODE_VERSION={{NODE_VERSION}}
GO_VERSION={{GO_VERSION}}
RUST_VERSION={{RUST_VERSION}}
JAVA_VERSION={{JAVA_VERSION}}
RUBY_VERSION={{RUBY_VERSION}}
PHP_VERSION={{PHP_VERSION}}

# Debian configuration
DEBIAN_FRONTEND=noninteractive

# ============================================================================
# Runtime Environment
# ============================================================================

# Application environment
NODE_ENV={{NODE_ENV}}
RAILS_ENV={{RAILS_ENV}}
FLASK_ENV={{FLASK_ENV}}
DJANGO_SETTINGS_MODULE={{DJANGO_SETTINGS_MODULE}}

# Python configuration
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1
PYTHONPATH={{PYTHONPATH}}

# Go configuration
GOPATH={{GOPATH}}
GOBIN={{GOBIN}}
GOCACHE={{GOCACHE}}

# Rust configuration
RUST_BACKTRACE={{RUST_BACKTRACE}}
CARGO_HOME={{CARGO_HOME}}

# Java configuration
JAVA_HOME={{JAVA_HOME}}
MAVEN_HOME={{MAVEN_HOME}}
GRADLE_HOME={{GRADLE_HOME}}

# Ruby configuration
GEM_HOME={{GEM_HOME}}
BUNDLE_PATH={{BUNDLE_PATH}}

# System PATH
PATH={{PATH}}

# ============================================================================
# Database Configuration
# ============================================================================

# PostgreSQL
POSTGRES_USER={{POSTGRES_USER}}
POSTGRES_PASSWORD={{POSTGRES_PASSWORD}}  # Use input variable: ${input:dbPassword}
POSTGRES_DB={{POSTGRES_DB}}
POSTGRES_HOST={{POSTGRES_HOST}}
POSTGRES_PORT={{POSTGRES_PORT}}
POSTGRES_SSLMODE={{POSTGRES_SSLMODE}}
DATABASE_URL={{DATABASE_URL}}

# MySQL
MYSQL_ROOT_PASSWORD={{MYSQL_ROOT_PASSWORD}}
MYSQL_DATABASE={{MYSQL_DATABASE}}
MYSQL_USER={{MYSQL_USER}}
MYSQL_PASSWORD={{MYSQL_PASSWORD}}
MYSQL_HOST={{MYSQL_HOST}}
MYSQL_PORT={{MYSQL_PORT}}

# MongoDB
MONGO_INITDB_ROOT_USERNAME={{MONGO_INITDB_ROOT_USERNAME}}
MONGO_INITDB_ROOT_PASSWORD={{MONGO_INITDB_ROOT_PASSWORD}}
MONGO_INITDB_DATABASE={{MONGO_INITDB_DATABASE}}
MONGO_HOST={{MONGO_HOST}}
MONGO_PORT={{MONGO_PORT}}
MONGODB_URI={{MONGODB_URI}}

# SQLite
SQLITE_DB_PATH={{SQLITE_DB_PATH}}

# MariaDB
MARIADB_ROOT_PASSWORD={{MARIADB_ROOT_PASSWORD}}
MARIADB_DATABASE={{MARIADB_DATABASE}}
MARIADB_USER={{MARIADB_USER}}
MARIADB_PASSWORD={{MARIADB_PASSWORD}}

# ============================================================================
# Cache Configuration
# ============================================================================

# Redis
REDIS_HOST={{REDIS_HOST}}
REDIS_PORT={{REDIS_PORT}}
REDIS_PASSWORD={{REDIS_PASSWORD}}  # Use Docker secret or input variable
REDIS_DB={{REDIS_DB}}
REDIS_URL={{REDIS_URL}}

# Memcached
MEMCACHED_HOST={{MEMCACHED_HOST}}
MEMCACHED_PORT={{MEMCACHED_PORT}}
MEMCACHED_SERVERS={{MEMCACHED_SERVERS}}

# ============================================================================
# Message Queue and Event Streaming
# ============================================================================

# RabbitMQ
RABBITMQ_DEFAULT_USER={{RABBITMQ_DEFAULT_USER}}
RABBITMQ_DEFAULT_PASS={{RABBITMQ_DEFAULT_PASS}}
RABBITMQ_HOST={{RABBITMQ_HOST}}
RABBITMQ_PORT={{RABBITMQ_PORT}}
RABBITMQ_MANAGEMENT_PORT={{RABBITMQ_MANAGEMENT_PORT}}
RABBITMQ_URL={{RABBITMQ_URL}}

# Kafka
KAFKA_BROKER={{KAFKA_BROKER}}
KAFKA_ZOOKEEPER={{KAFKA_ZOOKEEPER}}
KAFKA_TOPIC={{KAFKA_TOPIC}}
KAFKA_GROUP_ID={{KAFKA_GROUP_ID}}

# NATS
NATS_URL={{NATS_URL}}
NATS_CLUSTER_ID={{NATS_CLUSTER_ID}}

# ============================================================================
# Search and Analytics
# ============================================================================

# Elasticsearch
ELASTICSEARCH_HOST={{ELASTICSEARCH_HOST}}
ELASTICSEARCH_PORT={{ELASTICSEARCH_PORT}}
ELASTICSEARCH_URL={{ELASTICSEARCH_URL}}

# Kibana
KIBANA_HOST={{KIBANA_HOST}}
KIBANA_PORT={{KIBANA_PORT}}

# ============================================================================
# Cloud Provider Configuration
# ============================================================================

# AWS - NEVER set credentials as ENV! Use secret mounts or host mount
AWS_REGION={{AWS_REGION}}
AWS_PROFILE={{AWS_PROFILE}}
# AWS_ACCESS_KEY_ID - USE DOCKER SECRET OR INPUT VARIABLE ONLY
# AWS_SECRET_ACCESS_KEY - USE DOCKER SECRET OR INPUT VARIABLE ONLY

# Google Cloud Platform - NEVER set key as ENV!
GCP_PROJECT_ID={{GCP_PROJECT_ID}}
GCP_REGION={{GCP_REGION}}
# GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/gcp_key.json

# Azure
AZURE_SUBSCRIPTION_ID={{AZURE_SUBSCRIPTION_ID}}
AZURE_TENANT_ID={{AZURE_TENANT_ID}}
AZURE_RESOURCE_GROUP={{AZURE_RESOURCE_GROUP}}

# DigitalOcean
DO_TOKEN={{DO_TOKEN}}  # Use input variable: ${input:doToken}
DO_REGION={{DO_REGION}}

# ============================================================================
# API Keys and Authentication
# ============================================================================

# CRITICAL: Use VS Code input variables with password:true for ALL API keys
# Never commit real API keys! These are placeholders for input variables

# Version Control
GITHUB_TOKEN={{GITHUB_TOKEN}}  # Use: ${input:githubToken}
GITLAB_TOKEN={{GITLAB_TOKEN}}  # Use: ${input:gitlabToken}
BITBUCKET_TOKEN={{BITBUCKET_TOKEN}}  # Use: ${input:bitbucketToken}

# AI/ML Services
OPENAI_API_KEY={{OPENAI_API_KEY}}  # Use: ${input:openaiKey}
ANTHROPIC_API_KEY={{ANTHROPIC_API_KEY}}  # Use: ${input:anthropicKey}
HUGGINGFACE_TOKEN={{HUGGINGFACE_TOKEN}}  # Use: ${input:huggingfaceToken}
COHERE_API_KEY={{COHERE_API_KEY}}

# Payment Processing
STRIPE_SECRET_KEY={{STRIPE_SECRET_KEY}}  # Use Docker secret
STRIPE_PUBLISHABLE_KEY={{STRIPE_PUBLISHABLE_KEY}}
PAYPAL_CLIENT_ID={{PAYPAL_CLIENT_ID}}
PAYPAL_CLIENT_SECRET={{PAYPAL_CLIENT_SECRET}}  # Use Docker secret

# Communication Services
SENDGRID_API_KEY={{SENDGRID_API_KEY}}  # Use Docker secret
TWILIO_ACCOUNT_SID={{TWILIO_ACCOUNT_SID}}
TWILIO_AUTH_TOKEN={{TWILIO_AUTH_TOKEN}}  # Use Docker secret
SLACK_BOT_TOKEN={{SLACK_BOT_TOKEN}}  # Use input variable
DISCORD_BOT_TOKEN={{DISCORD_BOT_TOKEN}}

# Maps and Location
GOOGLE_MAPS_API_KEY={{GOOGLE_MAPS_API_KEY}}
MAPBOX_ACCESS_TOKEN={{MAPBOX_ACCESS_TOKEN}}

# Analytics and Monitoring
GOOGLE_ANALYTICS_ID={{GOOGLE_ANALYTICS_ID}}
MIXPANEL_TOKEN={{MIXPANEL_TOKEN}}
SEGMENT_WRITE_KEY={{SEGMENT_WRITE_KEY}}

# Error Tracking
SENTRY_DSN={{SENTRY_DSN}}  # Use: ${input:sentryDsn}
ROLLBAR_ACCESS_TOKEN={{ROLLBAR_ACCESS_TOKEN}}

# Package Registries (use Docker secrets during build!)
NPM_TOKEN={{NPM_TOKEN}}  # Mount as secret in Dockerfile
PYPI_TOKEN={{PYPI_TOKEN}}  # Mount as secret in Dockerfile
GEM_TOKEN={{GEM_TOKEN}}

# ============================================================================
# SSL/TLS Configuration
# ============================================================================

# SSL certificates (mount as Docker secrets)
SSL_CERT_PATH={{SSL_CERT_PATH}}
SSL_KEY_PATH={{SSL_KEY_PATH}}
SSL_CA_PATH={{SSL_CA_PATH}}

# ============================================================================
# Monitoring and Observability
# ============================================================================

# Logging
LOG_LEVEL={{LOG_LEVEL}}
LOG_FORMAT={{LOG_FORMAT}}
LOG_OUTPUT={{LOG_OUTPUT}}

# Metrics
PROMETHEUS_PORT={{PROMETHEUS_PORT}}
GRAFANA_ADMIN_PASSWORD={{GRAFANA_ADMIN_PASSWORD}}

# Tracing
JAEGER_AGENT_HOST={{JAEGER_AGENT_HOST}}
JAEGER_AGENT_PORT={{JAEGER_AGENT_PORT}}

# ============================================================================
# Application-Specific Configuration
# ============================================================================

# Application URLs
APP_URL={{APP_URL}}
API_URL={{API_URL}}
FRONTEND_URL={{FRONTEND_URL}}
BACKEND_URL={{BACKEND_URL}}

# Session and Security
SESSION_SECRET={{SESSION_SECRET}}  # Use input variable
JWT_SECRET={{JWT_SECRET}}  # Use Docker secret
ENCRYPTION_KEY={{ENCRYPTION_KEY}}  # Use Docker secret

# Feature Flags
FEATURE_FLAG_SERVICE={{FEATURE_FLAG_SERVICE}}
FEATURE_FLAGS={{FEATURE_FLAGS}}

# ============================================================================
# Development Tools
# ============================================================================

# Hot Reload
HOT_RELOAD={{HOT_RELOAD}}
WATCH_FILES={{WATCH_FILES}}

# Debug
DEBUG={{DEBUG}}
VERBOSE={{VERBOSE}}

# Testing
TEST_DATABASE_URL={{TEST_DATABASE_URL}}
COVERAGE_THRESHOLD={{COVERAGE_THRESHOLD}}

# ============================================================================
# Resource Limits (informational - set in docker-compose.yml)
# ============================================================================

# CPU_LIMIT={{CPU_LIMIT}}
# MEMORY_LIMIT={{MEMORY_LIMIT}}
# SWAP_LIMIT={{SWAP_LIMIT}}

# ============================================================================
# Custom Application Variables
# ============================================================================

# Add your custom variables below
# CUSTOM_VAR_1={{CUSTOM_VAR_1}}
# CUSTOM_VAR_2={{CUSTOM_VAR_2}}
# CUSTOM_VAR_3={{CUSTOM_VAR_3}}

# ============================================================================
# SECURITY CHECKLIST - COMPLETE BEFORE PRODUCTION!
# ============================================================================

# [ ] Add .env to .gitignore
# [ ] Replace ALL {{PLACEHOLDER}} values
# [ ] Remove unused sections
# [ ] Change ALL default passwords
# [ ] Configure VS Code input variables for API keys
# [ ] Set up Docker secrets for sensitive credentials
# [ ] Mount cloud provider credentials from host
# [ ] Enable strict firewall mode
# [ ] Configure custom firewall allowlist
# [ ] Review and update allowed domains
# [ ] Test credential rotation procedures
# [ ] Document required secrets for team
# [ ] Set up secret management system (Vault, Secrets Manager, etc.)
# [ ] Configure MCP servers with proper authentication
# [ ] Review container resource limits
# [ ] Set up monitoring and alerting
# [ ] Test backup and recovery procedures

# ============================================================================
# Documentation References
# ============================================================================

# For comprehensive guidance:
# - docs/VARIABLES.md - Variable types and usage patterns
# - docs/SECRETS.md - Secure credential handling
# - docs/MODES.md - Mode comparison and selection
# - docs/security-model.md - Overall security architecture
# - data/variables.json - Complete variable catalog
# - data/secrets.json - Secret handling patterns

# YOLO Mode = Maximum Control + Maximum Responsibility
# With great power comes great responsibility - secure your credentials!
>>>>>>> dc2424c2457c6bf1fc281678bfedb2e3930d7a62
