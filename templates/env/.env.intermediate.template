<<<<<<< HEAD
# Intermediate Mode Environment Template
# Standard development configuration with message queue support
# Copy to .env and customize if needed

# ============================================================================
# Container Configuration
# ============================================================================

# Timezone
TZ=UTC

# Firewall mode (disabled, permissive, strict)
FIREWALL_MODE=permissive

# Claude Code version
CLAUDE_CODE_VERSION=latest

# ============================================================================
# Build Configuration
# ============================================================================

# Language versions (customizable via docker-compose build args)
PYTHON_VERSION=3.12
NODE_VERSION=20

# ============================================================================
# Runtime Environment
# ============================================================================

# Node.js environment
NODE_ENV=development

# Python configuration
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================

# PostgreSQL connection
POSTGRES_USER=sandbox_user
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=sandbox_dev
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Full database URL (constructed from components above)
# NOTE: For passwords with special characters (@, :, /, etc.), set DATABASE_URL directly
# with URL-encoded values, or the application will handle encoding automatically
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ============================================================================
# Alternative Databases (Optional)
# ============================================================================

# MySQL (uncomment if using)
# MYSQL_ROOT_PASSWORD=rootpassword
# MYSQL_DATABASE=sandbox_dev
# MYSQL_USER=sandbox_user
# MYSQL_PASSWORD=devpassword

# MongoDB (uncomment if using)
# MONGO_INITDB_ROOT_USERNAME=root
# MONGO_INITDB_ROOT_PASSWORD=rootpassword
# MONGO_INITDB_DATABASE=sandbox_dev

# ============================================================================
# Cache Configuration (Redis)
# ============================================================================

# Redis connection
REDIS_HOST=redis
REDIS_PORT=6379

# Memcached (uncomment if using)
# MEMCACHED_HOST=memcached
# MEMCACHED_PORT=11211

# ============================================================================
# Message Queue Configuration (RabbitMQ)
# ============================================================================

# RabbitMQ connection
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# ============================================================================
# Git Authentication (VS Code input variables recommended)
# ============================================================================

# GitHub token (use VS Code input variable with password:true)
# GITHUB_TOKEN=${input:githubToken}
# See devcontainer.json for input variable configuration

# ============================================================================
# Security Notes
# ============================================================================

# WARNING: These are development defaults only!
# - Never commit real passwords or tokens to Git
# - Change default credentials (especially RabbitMQ guest/guest)
# - Use VS Code input variables for Git tokens
# - Add .env to .gitignore

# For better security, see:
# - Advanced mode for strict firewall and Docker secrets
# - docs/SECRETS.md for secure credential handling
# - docs/VARIABLES.md for VS Code input variable patterns
=======
# Intermediate Mode Environment Template
# Standard development configuration with message queue support
# Copy to .env and customize if needed

# ============================================================================
# Container Configuration
# ============================================================================

# Timezone
TZ=UTC

# Firewall mode (disabled, permissive, strict)
FIREWALL_MODE=permissive

# Claude Code version
CLAUDE_CODE_VERSION=latest

# ============================================================================
# Build Configuration
# ============================================================================

# Language versions (customizable via docker-compose build args)
PYTHON_VERSION=3.12
NODE_VERSION=20

# ============================================================================
# Runtime Environment
# ============================================================================

# Node.js environment
NODE_ENV=development

# Python configuration
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================

# PostgreSQL connection
POSTGRES_USER=sandbox_user
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=sandbox_dev
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Full database URL
DATABASE_URL=postgresql://sandbox_user:devpassword@postgres:5432/sandbox_dev

# ============================================================================
# Alternative Databases (Optional)
# ============================================================================

# MySQL (uncomment if using)
# MYSQL_ROOT_PASSWORD=rootpassword
# MYSQL_DATABASE=sandbox_dev
# MYSQL_USER=sandbox_user
# MYSQL_PASSWORD=devpassword

# MongoDB (uncomment if using)
# MONGO_INITDB_ROOT_USERNAME=root
# MONGO_INITDB_ROOT_PASSWORD=rootpassword
# MONGO_INITDB_DATABASE=sandbox_dev

# ============================================================================
# Cache Configuration (Redis)
# ============================================================================

# Redis connection
REDIS_HOST=redis
REDIS_PORT=6379

# Memcached (uncomment if using)
# MEMCACHED_HOST=memcached
# MEMCACHED_PORT=11211

# ============================================================================
# Message Queue Configuration (RabbitMQ)
# ============================================================================

# RabbitMQ connection
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# ============================================================================
# Git Authentication (VS Code input variables recommended)
# ============================================================================

# GitHub token (use VS Code input variable with password:true)
# GITHUB_TOKEN=${input:githubToken}
# See devcontainer.json for input variable configuration

# ============================================================================
# Security Notes
# ============================================================================

# WARNING: These are development defaults only!
# - Never commit real passwords or tokens to Git
# - Change default credentials (especially RabbitMQ guest/guest)
# - Use VS Code input variables for Git tokens
# - Add .env to .gitignore

# For better security, see:
# - Advanced mode for strict firewall and Docker secrets
# - docs/SECRETS.md for secure credential handling
# - docs/VARIABLES.md for VS Code input variable patterns
>>>>>>> dc2424c2457c6bf1fc281678bfedb2e3930d7a62
