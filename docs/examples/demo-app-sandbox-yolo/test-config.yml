metadata:
  config: custom
  description: "Custom configuration with Python backend, comprehensive configuration, full control"
  expected_files:
    - .devcontainer/devcontainer.json
    - .devcontainer/Dockerfile
    - docker-compose.yml
    - .devcontainer/init-firewall.sh
    - .devcontainer/setup-claude-credentials.sh
    - .devcontainer/.bashrc
    - .devcontainer/.editorconfig

responses:
  - prompt_pattern: "project.*name|what.*call"
    response: "demo-app"

  - prompt_pattern: "language|stack"
    response: "python"

  - prompt_pattern: "confirm|proceed"
    response: "yes"
