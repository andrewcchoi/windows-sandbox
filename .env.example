# ============================================================================
# Claude Code Sandbox - Environment Configuration Template
# ============================================================================
# Copy this file to .env and customize for your environment.
# For mode-specific templates, see: templates/env/
#
# Mode Templates Available:
#   - templates/env/.env.basic.template
#   - templates/env/.env.intermediate.template
#   - templates/env/.env.advanced.template
#   - templates/env/.env.yolo.template
# ============================================================================

# ============================================================================
# Container Configuration
# ============================================================================
TZ=UTC
# FIREWALL_MODE=strict  # Options: disabled, permissive, strict

# ============================================================================
# Database Configuration (PostgreSQL)
# ============================================================================
POSTGRES_USER=sandbox_user
POSTGRES_PASSWORD=devpassword
POSTGRES_DB=sandbox_dev
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Full database URL (constructed from components above)
# NOTE: For passwords with special characters (@, :, /, etc.), set DATABASE_URL directly
# with URL-encoded values, or the application will handle encoding automatically
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ============================================================================
# Cache Configuration (Redis)
# ============================================================================
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=devpassword  # Uncomment if using password

# ============================================================================
# Message Queue (RabbitMQ) - Optional
# ============================================================================
# RABBITMQ_DEFAULT_USER=guest
# RABBITMQ_DEFAULT_PASS=guest
# RABBITMQ_HOST=rabbitmq
# RABBITMQ_PORT=5672

# ============================================================================
# Security Notes
# ============================================================================
# WARNING: These are development defaults only!
# - Never commit real passwords to Git
# - Change passwords for production environments
# - Add .env to .gitignore (already done)
#
# For production security, see: docs/SECRETS.md
# ============================================================================
