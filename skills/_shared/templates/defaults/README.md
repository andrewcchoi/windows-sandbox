# Defaults Directory

This directory contains default configurations that are copied into new DevContainer environments.

## Structure

- `hooks/` - Default Claude Code hooks
- `state/` - Default state files
- `plugins/` - Default plugins to install

## Usage

These defaults are used by `setup-claude-credentials.sh` when initializing a new container.
Place any default configurations in the appropriate subdirectory.
